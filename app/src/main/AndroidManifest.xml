<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coursedesigner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_cd"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_cd"
        android:supportsRtl="true"
        android:theme="@style/Theme.CourseDesigner">
        <activity android:name=".Galerie"/>
        <activity android:name=".ScrollingExplication" />
        <activity android:name=".Course"
            android:screenOrientation="landscape"/>
        <activity android:name=".Accueil" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>